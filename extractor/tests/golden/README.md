# Golden files

Drop real university PDFs here (`some-name.pdf`) alongside their expected
extraction output (`some-name.json`, matching `extract.py`'s output shape).
`test_golden.py` currently has no fixtures to run against — add at least
one date-sheet/table-heavy PDF before relying on this test as a safety net.
