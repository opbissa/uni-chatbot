{
  "name": "widget",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.js --watch",
    "build": "node esbuild.config.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
